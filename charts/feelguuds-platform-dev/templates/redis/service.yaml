{{- if .Values.redis.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "feelguuds-platform.fullname" . }}-redis
  labels:
    app: {{ template "feelguuds-platform.fullname" . }}-redis
spec:
  type: ClusterIP
  selector:
    app: {{ template "feelguuds-platform.fullname" . }}-redis
  ports:
    - name: redis
      port: 6379
      protocol: TCP
      targetPort: redis
{{- end }}
